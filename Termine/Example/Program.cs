using System;
namespace Example
{
    class Program
    {
        // the only code the user has to type
        static void Main(string[] args)
        {
            Game game = new Game();
            Console.ReadKey(true);
        }
    }
}
